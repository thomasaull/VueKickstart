overwrite: true
# schema: "http://localhost:8000/graphql"
schema: './generated/schema.graphql.json'
documents: 'src/**/*.graphql'
generates:
  generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
      # - fragment-matcher